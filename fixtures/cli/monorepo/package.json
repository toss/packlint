{
  "name": "test-monorepo",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo build",
    "test": "turbo test",
    "lint": "packlint"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
